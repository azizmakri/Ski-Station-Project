version: "3"
services:
  skistationproject:
    image: skistationimage
    ports:
      - "5052:8080"
    networks:
      - networkSki

  mysqldb:
    image: mysql:8
    networks:
      - networkSki
    environment:
      - MYSQL_DATABASE=MohamedAzizMakri

networks:
  networkSki:

version: "3"
services:
  skistationproject:
    image: skistation3
    ports:
      - "5050:8080"
    networks:
      - networkSki

  mysqldb:
    image: mysql
    networks:
      - networkSki
    environment:
      - MYSQL_DATABASE=SkiStationDBamir

networks:
  networkSki:
